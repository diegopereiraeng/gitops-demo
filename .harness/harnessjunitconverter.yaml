inputSet:
  name: harness-junit-converter
  identifier: harnessjunitconverter
  orgIdentifier: default
  projectIdentifier: GIT_FLOW_DEMO
  pipeline:
    identifier: Plugin_Factory
    template:
      templateInputs:
        properties:
          ci:
            codebase:
              build:
                type: branch
                spec:
                  branch: main
        variables:
          - name: plugin_name
            type: String
            value: harness-junit-converter
          - name: repo_name
            type: String
            value: harness-junit-converter
          - name: image_name
            type: String
            value: harness-junit-converter
  object Object: pipeline.template.templateInputs.properties.ci.codebase
